
        procedure foo (a,b: integer);
        var x,y: real;
        begin
        end

        procedure main();
        //this is a comment
        var a,b: integer;
            c:array [1 .. 2] of real;
        begin
            a:= 1;
            if a>1 then
                foo(a,b);
            else 
                a := foo(a,b)+3;
            while a=1 do a:=c-b;
            for i:=1 to a*2 do foo(a,b);
            break;
            continue;
            return foo(a,b);
            with a:integer; b:real; do
            begin
                a:=foo(a,b);
                print(a);
            end

        end
        